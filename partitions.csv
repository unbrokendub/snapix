# Name,    Type, SubType,  Offset,   Size,     Flags
# v2.0.60: dropped OTA (otadata + app1) to give the entire former OTA-slot
# range to the LittleFS partition.  Page cache will migrate from SD onto
# this enlarged LittleFS in Phase 2 — see roadmap in commit notes.
#
# Layout map (16 MB total flash):
#   0x000000-0x009000   bootloader (36 KB, fixed)
#   0x009000-0x00F000   nvs (24 KB) — bumped from 20 KB to absorb the freed
#                        otadata window cleanly without realigning factory
#   0x00F000-0x010000   gap (4 KB) — factory must be 64-KB-aligned
#   0x010000-0x650000   factory app (6.25 MB) — same address & size as the
#                        old app0, so an in-place reflash boots normally
#   0x650000-0xFF0000   spiffs/LittleFS (9.625 MB) — was 3.375 MB, now
#                        absorbs the entire 6.25 MB former-app1 region
#   0xFF0000-0x1000000  coredump (64 KB)
nvs,       data, nvs,      0x9000,   0x6000,
factory,   app,  factory,  0x10000,  0x640000,
spiffs,    data, spiffs,   0x650000, 0x9A0000,
coredump,  data, coredump, 0xFF0000, 0x10000,
